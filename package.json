{
  "type": "module",
  "scripts": {
    "dev-server": "dufs --render-index",
    "dev-css": "tailwindcss -i input.css -o style.css --watch --no-autoprefixer",
    "build": "node build.js",
    "serve": "dufs --render-index dist"
  },
  "devDependencies": {
    "html-minifier-terser": "^7.2.0",
    "tailwindcss": "^3.4.1"
  }
}
